FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir 'granian>=1,<2' 'starlette'
COPY app.py /app/app.py
EXPOSE 8000
CMD ["granian", "--interface", "asgi", "--host", "0.0.0.0", "--port", "8000", "--workers", "1", "--log-level", "warning", "app:app"]
